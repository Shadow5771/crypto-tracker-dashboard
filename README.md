# Crypto Tracker — Improved (Ready-to-run)

## Setup
1. Create & activate virtualenv
   - Windows:
     ```
     python -m venv venv
     venv\Scripts\activate
     ```
   - Linux/macOS:
     ```
     python -m venv venv
     source venv/bin/activate
     ```

2. Install dependencies:
